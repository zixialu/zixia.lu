import PropTypes from 'prop-types';
import React from 'react';

import './sidebar.scss';

const Sidebar = () => (
  <div>Sidebar</div>
);

Sidebar.propTypes = {
};

Sidebar.defaultProps = {
};

export default Sidebar;
